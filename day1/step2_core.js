/**
 * var => object scope +  hoisting
 * let => block scope
 * const => block scope + constants that can not be changed + always need to be initialized when you create it
 */

/*
function myfunc(){
    console.log("Value of index before the loop ", index); // hoisting

    for (var index = 0; index < 10; index++) {
        console.log(index);
    }

    console.log("value of index after the loop ", index); //block  and object scope
}

myfunc();
*/

/*
const user = "padmaja"; // constants can not be changed
    user = "amar";

console.log(user);

*/

